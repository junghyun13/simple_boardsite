spring:
  datasource:
    url: jdbc:mysql://localhost/simpleDb_test?useSSL=false&serverTimezone=UTC
    username: root
    password: 1213
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  logging:
    level:
      org.springframework.transaction: DEBUG
      org.springframework.jdbc: DEBUG



